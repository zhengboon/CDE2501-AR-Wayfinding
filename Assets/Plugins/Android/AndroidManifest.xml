<?xml version="1.0" encoding="utf-8"?>
<!--
    Custom AndroidManifest.xml for CDE2501 AR Wayfinding.
    Unity merges this with its generated manifest at build time.
    The FileProvider <provider> block is required for Android 7+ (API 24+)
    to share files across apps via content:// URIs.
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unity3d.player"
    android:installLocation="preferExternal">

    <!-- Permissions required by this app -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Camera hardware feature -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <!-- Keep AR optional so non-ARCore phones can still launch the app. -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false"
        tools:node="replace"
        tools:replace="android:required" />
    <uses-feature
        android:name="com.google.ar.core.depth"
        android:required="false"
        tools:node="replace"
        tools:replace="android:required" />

    <application>
        <!--
            Unity launcher activity.
            Required for app icon visibility and "Open" button after install.
        -->
        <activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@style/UnityThemeSelector"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density|colorMode"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>

        <!-- Override ARCore plugin default from required -> optional. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional"
            tools:replace="android:value" />

        <!--
            FileProvider — required for cross-app file sharing on Android 7+ (API 24+).
            The authority MUST match the string constructed in DataSyncManager.ShareOnAndroid():
                context.getPackageName() + ".fileprovider"
            Unity replaces applicationId at build time so this stays generic.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
